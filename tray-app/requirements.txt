pystray>=0.19.5
pillow>=10.0.0
six

# Only install these if the OS is macOS
pyobjc-core>=12.0; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=12.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=7.0; sys_platform == 'darwin'